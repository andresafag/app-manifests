apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast-storage
provisioner: ebs.csi.aws.com
parameters:
  type: gp3
reclaimPolicy: Delete # Retain mantiene los datos si borras el PVC; Delete los borra
volumeBindingMode: WaitForFirstConsumer
