apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ebs-sc
provisioner: ebs.csi.aws.com # Driver de AWS
volumeBindingMode: WaitForFirstConsumer # Recomendado para multizona
parameters:
  type: gp3
  encrypted: "true"
